package com.clariflow.workflow.model.enums;

/**
 * Priority level enumeration.
 */
public enum Priority {

    /** P0 — Critical, must be resolved immediately. */
    P0,

    /** P1 — High priority, should be resolved soon. */
    P1,

    /** P2 — Normal priority, standard workflow. */
    P2
}
